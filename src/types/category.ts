export interface IsportCategory {
    _id: string;
    name: string;
}
export interface IigniteTeam {
    _id: string;
    name: string;
    position: string;
    profile_image: string;
    createdAt: string;
    updatedAt: string;
}
